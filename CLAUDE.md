# Workspace — d:\5. Code & AI

## About Me
- Beginner developer, coming from a language services background (not tech)
- Windows 11, VS Code, bash shell
- Main goal: build cool products and learn things along the way
- My name is Fabricio

## New Projects
- Template lives at: 4. Tools/Templates/Web Project/
- When Fabricio asks to create a new project, before creating anything ask these questions:
  1. What does this project do? (one sentence)
  2. Who is it for?
  3. What problem does it solve?
- Only create the project once all answers are collected
- Use the answers to write a rich CLAUDE.md for the new project
- Naming convention: "Number. Project Name" (e.g. 1. IRL-Quests, 2. Next Project)
- Pick the next number in sequence automatically

## Project Folder Structure
- Always group files by type into numbered subfolders
- Naming convention: "1. FolderName" — number first, then a clear label
- Files of the same type or purpose always go in the same folder
- Common folders to create as needed (use what makes sense for the project):
  - 1. HTML — markup/template files
  - 2. Images — exported images, screenshots, assets
  - 3. Scripts — JS, Python, or other scripts
  - 4. Data — raw data files (GPX, JSON, CSV, etc.)
  - 5. Styles — standalone CSS files (if not inside HTML)
  - 6. Docs — notes, references, planning files
- Root of the project folder: CLAUDE.md, package.json, config files, dependency folders (node_modules etc.), and index.html if the project is deployed via GitHub Pages (GitHub Pages requires index.html at the root)
- For GitHub Pages projects, 1. HTML/ is used for secondary pages only — the main index.html stays at the root
- When adding new files mid-project, always place them in the correct subfolder, never dump them in the root

## Workspace Structure
- 1. Learning/ — courses, notes, AI and language experiments
- 2. Projects/ — real projects (active: IRL-Quests)
- 3. Resources/ — references, assets
- 4. Tools/ — scripts, utilities

## Learning Notes
- Notes live in 1. Learning/Notes/ organized by topic (JavaScript, Firebase, HTML-CSS, Tools, AI, General)
- The main notes page is at: 1. Learning/notes.html (use /add-note to add entries, /add-project to add projects)
- Each note follows this template: What it is / What I learned / Example / Source
- During any session — after fixing a bug, learning a concept, finishing a feature, or when something non-obvious comes up — ask Fabricio: "Worth saving this as a note?"
- Don't wait until the end of a session. Ask in the moment, right after the thing is explained or solved.

## How I Like to Work
- No jargon. Write at an 8th grade reading level
- Be direct. Ask questions if something is not clear
- Explain *why* briefly when making decision
- Flag things I should understand, not just fix them

## Tools & Cost
- Free tools only. Do not suggest paid APIs or services
- If a paid option would make a significant difference to the current project, you can mention it once clearly

## Active Stack
- Vanilla JS, Firebase, Node.js (IRL-Quests project), remotion
